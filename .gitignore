.DS_Store
node_modules/
*.log
.vscode/
.idea/

# Local-only resume sources. Editable copies used to generate the PDFs in assets/.
# Kept out of git so they aren't redeployed to the public site.
resume.html
resume-visual.html

# Reference / alternate resume PDFs kept local (not served on the public site).
assets/Resume-old.pdf
assets/Rishabh-Gandotra-Resume-ATS.pdf

# Internal source decks used as reference material for case-study writing.
# Contain internal-only detail (JIRA IDs, scope planning) — not for the public repo.
assets/vmware-cloud-on-dell/Case study presentation - Dell.pdf
assets/vmware-cloud-on-dell/Outposts UX scope.pdf
assets/vmware-cloud-on-dell/VMC on AWS Outposts.pdf

# Raw keynote photos -- superseded by the resized/renamed vmc-keynote-*.jpg
# files actually used on the site. Kept locally in case a different crop is
# ever wanted, not for the public repo.
assets/vmware-cloud-on-dell/IMG_20180827_*.jpg
